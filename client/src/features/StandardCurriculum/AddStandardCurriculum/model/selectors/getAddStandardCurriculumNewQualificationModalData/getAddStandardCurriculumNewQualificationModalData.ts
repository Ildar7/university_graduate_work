import { StateSchema } from 'app/providers/StoreProvider';

export const getAddStandardCurriculumNewQualificationModalData = (state: StateSchema) => state.addStandardCurriculum?.newQualificationModalData;
